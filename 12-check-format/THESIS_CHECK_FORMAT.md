# THESIS_CHECK_FORMAT

Run the deterministic format checker.

```bash
python 12-check-format/check_format.py --project-root <latex-project> --ruleset <ruleset>
```

It checks figure/table blocks, label/ref integrity, and front-matter requirements defined by the selected rule pack.
