# THESIS_CHECK_LANGUAGE

Run the deterministic language checker.

```bash
python 11-check-language/check_language.py --project-root <latex-project> --ruleset <ruleset>
```

It respects `90-rules/packs/<ruleset>/rules.yaml` for spacing, punctuation, quote style, and weak-phrase checks.
