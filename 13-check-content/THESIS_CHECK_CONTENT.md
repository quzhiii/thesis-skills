# THESIS_CHECK_CONTENT

Run the deterministic content-structure checker.

```bash
python 13-check-content/check_content.py --project-root <latex-project> --ruleset <ruleset>
```

It checks required section presence, abstract keyword counts, and acronym candidates.
