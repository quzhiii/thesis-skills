# THESIS_CHECK_REFERENCES

Run the deterministic reference checker.

```bash
python 10-check-references/check_references.py --project-root <latex-project> --ruleset <ruleset>
```

It validates `\cite{}` keys against bibliography files declared by the pack.
